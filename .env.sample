# .env.sample - copy to .env.local (or .env) and fill values

# Next.js public envs (exposed to browser). Use `NEXT_PUBLIC_` prefix.
# Google Maps / Forge proxy key (client-side)
NEXT_PUBLIC_FRONTEND_FORGE_API_KEY=YOUR_GOOGLE_MAPS_API_KEY
# Optional: override the default Forge API base URL (defaults to https://forge.butterfly-effect.dev)
NEXT_PUBLIC_FRONTEND_FORGE_API_URL=https://forge.butterfly-effect.dev

# OAuth / App configuration (used by client-side code that builds login URL)
NEXT_PUBLIC_OAUTH_PORTAL_URL=https://your-oauth-portal.example.com
NEXT_PUBLIC_APP_ID=your_app_id

# Backwards compatibility: some files currently read VITE_* (from import.meta.env).
# If you don't update code yet, you can keep VITE_ aliases alongside NEXT_PUBLIC_.
# Example (optional):
# VITE_FRONTEND_FORGE_API_KEY=$NEXT_PUBLIC_FRONTEND_FORGE_API_KEY
# VITE_FRONTEND_FORGE_API_URL=$NEXT_PUBLIC_FRONTEND_FORGE_API_URL
# VITE_OAUTH_PORTAL_URL=$NEXT_PUBLIC_OAUTH_PORTAL_URL
# VITE_APP_ID=$NEXT_PUBLIC_APP_ID

# Common Node/Next variables (optional)
# NODE_ENV=development
# PORT=3000

# Notes:
# - In Next.js, only variables prefixed with NEXT_PUBLIC_ are available in the browser.
# - Replace occurrences of `import.meta.env.VITE_*` with `process.env.NEXT_PUBLIC_*`
#   or add the VITE_ aliases during migration to avoid runtime breakage.
